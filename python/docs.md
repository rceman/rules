# 📑 Documentation Rules

## README.md **Must** Contain

* Project purpose – one‑sentence elevator pitch.
* Quick start – three‑line snippet: `git clone`, `make setup`, `make run`.
* Examples / API usage.
* Contribution pointer.

## Extended Docs

Use **MkDocs** with Material theme *or* **Sphinx**.

* Build docs under `docs/`.
* Ignore `docs/site/` or `_build/` in git.
